# Enforce LF (Unix) line endings for all shell scripts
*.sh text eol=lf

# Enforce CRLF (Windows) line endings for all batch scripts
*.bat text eol=crlf

# Auto-detect other text files and normalize them
* text=auto